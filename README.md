# speech-phoneme-analysis-pipeline
A pipeline for phoneme-level speech analysis. It parses TextGrid files to extract phoneme timings and metadata, builds a structured dataset, and supports downstream experiments on phoneme representations across speakers and L1/L2 language backgrounds using neural speech models.
